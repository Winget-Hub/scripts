# Bloxburg 2 Junk Drawer Notes

Dirty-only capture. This folder keeps the original small entrypoint, local library, and large loadstring payload because no clean recovery folder existed in `Scripts/Skids`.
