# Backdoor-darkz Recovery Report

## Files

- Main recovery: `Main.lua`

## Status

Partial MoonSec-style recovery. The clean file reconstructs the visible DarkX key GUI, Work.ink key URL, token validation call, settings file use, and the point where execution moves into an unrecovered embedded payload.

## Notes

The original protected payload did not expose complete readable source during the local probes, so this entry is a reviewable behavior reconstruction rather than a full original-source recovery.
