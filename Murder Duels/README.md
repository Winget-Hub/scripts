# Murder Duels

## Status

Combined readable source.

## Files

- `Main.lua` - combined script source with the remote UI dependency included.

## Notes

The original script depended on a remote UI library, which made review harder because important behavior lived outside the visible entry file. The archived copy keeps the relevant code together in one file.
